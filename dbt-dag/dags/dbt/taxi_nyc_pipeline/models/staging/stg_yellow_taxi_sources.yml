version: 2

sources:
  - name: raw_taxi_data
    description: "Données brutes Taxi NYC chargées depuis ..."
    database: NYC_TAXI_DB
    schema: RAW
    tables:
      - name: YELLOW_TAXI_TRIPS
        columns:
          - name: VendorID
            tests:
              - not_null
              - accepted_values:
                  values: [0, 1, 2]

        